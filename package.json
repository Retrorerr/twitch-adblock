{
  "name": "twitch-vaft-ublock-resource",
  "version": "0.1.0",
  "private": true,
  "description": "Personal VAFT-style Twitch script packaging for uBlock Origin.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --check scripts/build.js && node scripts/build.js"
  }
}
